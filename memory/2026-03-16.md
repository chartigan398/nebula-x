# 2026-03-16 — Nebula X

- Palette: fixed **20s real-time** segments, **ordered cycle** (spectrum: purple → blue → cyan → green → yellow → orange → red → magenta → white neutral), no random dwell. `colorSpeed` scales segment length (20s / speed).
- Motion: **session seed + phase offsets** + **wall-clock drift** on `flowTime` so runs aren’t identical; aligns with blueprint §4.1 (non-repeating flow evolution).

## Narrative §7 v0

- Four scenes (Void → Current → Ember → Aurora), **120s hold** + **36s crossfade** each, loops. **N** toggles; `STORAGE_NARRATIVE`. Blends fog, hue shift, morph biases, flow/bloom/camera multipliers, sat/light. Mild **midNorm** time nudge when audio on.

## Later same day — §3.2 + §4.2–4.3

- **Dual-timescale:** Focus vs Sleep now have different `bassSlow/midSlow/highSlow`, bloom attack/release, reform rate, pad damp on onset.
- **Envelopes:** `bloomEnv` (transient bloom) + `reformMagnet` (post-hit pull); normalized bands use **slow** envelopes.
- **Caps:** max stray from morph target, max bloom push, max shimmer units, flow vector cap — per mode.
- Debug `D`: shows `bloomEnv` + `reformMagnet`.

## Blueprint pass — §4.1 §5 §6 §8

- **Palette:** third mode **sleep** (warm amber/red); **P** cycles spectrum → reactor → sleep. **Long-form arcs:** `PALETTE_ARC_MULT=3` (~60s segments at default color speed). **Session lock:** `STORAGE_PALETTE_LOCK_IDX` fixes starting segment for tab session.
- **Modes:** focus gets `highlightHueShift`, `detailCadence`, `pulseReadScale`; sleep gets lower `velocityVariance`, slower `detailCadence`, warmer `highlightHueShift`.
- **Simulation:** second flow layer (`nBlend`), `hueBase` for highlights; sleep shares spectrum color path with explicit `paletteMode === 'sleep'`.
- **Stability:** `disposeAudioResources()` + mic `MediaStream` track stop + revoke file blob URL; **hidden tab** throttles main loop to ~2 FPS; `ensureAudioGraph` resumes suspended context.
- **Sleep 60m** creator pack uses `paletteMode: 'sleep'`.

## Continued — §0 §1.2 §2 docs + sparklines

- **`README.md`**, **`ARCHITECTURE.md`**, **`docs/GPU_ROADMAP.md`** — mission line, telemetry rules, WebGPU buffer/compute contract.
- **FFT reuse:** `fftByteFrequency` buffer — no per-frame `new Uint8Array` in `computeAudioBands`.
- **Sparkline UI:** three canvases (bass / mid / flatness) + `drawSparkline` / `drawAllSparklines` each frame.
- **Debug `D`:** §1.2 telemetry block (envelope drivers).

## GPU vertex path (§2 “done”)

- **`js/gpu-points-material.js`:** full morph + magnet + flow + caps + bloom + HSL + size in **GLSL**; **`UniformsLib.common`** for matrices.
- **`render-core.js`:** `cloudPos` / `ringPos` / `spherePos` / `phase` **BufferAttributes**; `points.frustumCulled = false`.
- **`index.html`:** GPU **opt-in** (`localStorage` `nebula-x-gpu-particles === 'true'`); checkbox + reload. **`D`** shows `gpuParticles: vertex shader | CPU`.

## Carry on — §9 export + GPU UX + docs

- **CPU default** for GPU flag; **point size** `uSizeScale` + doc fixes landed earlier.
- **Export session log (JSON)** button → `CreatorPresets.readLog()` download.
- **GPU shader compile fail:** clear `localStorage` GPU flag + status line “using CPU sim”.
- **`BLUEPRINT_STATUS.md`** rewritten; **`VERSION.md`** v6 stub; **`MODULES.md`** notes export.

## Beast build — tiers + profile + help + FPS

- **`js/user-profile.js`:** Save/Load **beast profile** (`nebula-x-beast-profile`).
- **Tiers:** Lite 40k / Standard 100k / Beast 200k — `nebula-x-particle-tier`, reload.
- **`?` / Shift+/` help overlay; **Esc** / backdrop click to close.
- **`StabilityMonitor.getFPS()`** — debug line `fps` + `particles`.

## v6 ship polish (same day)

- **Fixed camera** — no orbit, no mouse-on-camera; **Camera** slider + `cameraSpeed` / `cameraScale` removed. Particles + sim only move.
- **Product polish:** page `<title>` + meta description + theme-color + inline SVG favicon; removed dead `#ui` CSS; help + footer stamp **Nebula X v6**; `README` / `VERSION.md` / `BLUEPRINT_STATUS` ship checklist updated.

## Expression + MIR wiring (visible difference)

- **`params.visualExpress` + panel slider** (0.5–2): morph sin/cos rate, mid→morph authority, bloom/onset point size, idle breath amplitude, rotation with audio, canvas `brightness`/`saturate` when audio on.
- **`simulation-core.js`:** **centroid → global hue drift** (`euclideanModulo`); **flatness** scales flow `nBlend`; **spread** scales stray cap; stronger idle breath + rotation.
- **Presets / creator packs** set different `visualExpress`.
- **Bugfix:** health tracker passed undefined `timeDomain` → `timeDomain: timeDomainData`.
- **GPU path:** same idle breath, rotation, size formula (centroid hue still CPU-shader only until GPU uniform added).
