{
  "name": "the-titan",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "WebGPU-first visual engine — curl noise, compute particles, director layer (greenfield)",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "three": "^0.173.0"
  },
  "devDependencies": {
    "vite": "^6.0.0"
  }
}
